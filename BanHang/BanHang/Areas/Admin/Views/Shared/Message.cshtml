﻿@if (TempData.ContainsKey("message"))
    {

        BanHang.Library.XMessage message = (BanHang.Library.XMessage)TempData["message"];
        <div class="alert alert-@message.TypeMsg ">
            @message.Msg
        </div>

    }
